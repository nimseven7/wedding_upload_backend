services:
  web:
    build:
      context: .
      dockerfile: backend.Dockerfile
    ports:
      - "8090:8090"
    volumes:
      - ./uploads:/app/uploads
  #   depends_on:
  #     - db
  # db:
  #   image: postgres